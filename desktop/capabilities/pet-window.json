{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-window",
  "description": "Minimal core permissions for the dynamically-created SynthChat pet window.",
  "windows": ["pet"],
  "permissions": ["core:default"]
}
