{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal permissions for the SynthChat main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
